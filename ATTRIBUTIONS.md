# Attributions

This project includes components from [shadcn/ui](https://ui.shadcn.com/) used under [MIT license](https://github.com/shadcn-ui/ui/blob/main/LICENSE.md).

This project includes photos from [Unsplash](https://unsplash.com) used under [license](https://unsplash.com/license).

<!--
Note for maintainers: as of now the covered material isn't actually used by the
live app — shadcn/ui (`src/app/components/ui/`) is imported by nothing, and the
Unsplash photo referenced in `src/app/components/maple-shared.tsx` (the legislator
mock) is used only by the archived concept pages, not the live rent-control page.

The attributions above are kept active because that code and those assets are
still in the repo. If you ever delete `src/app/components/ui/` and remove the
Unsplash reference, you can drop the corresponding line above.
-->
